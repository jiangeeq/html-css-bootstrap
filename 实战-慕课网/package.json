{
  "name": "mukewang",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "live-server ./ --port=8081",
    "compile:sass": "sass sass/base.scss css/base.css -w --no-source-map",
    "compileFile:sass": "sass --watch sass/base.scss css/base.css -w --no-source-map",
    "compileFolder:sass": "sass --watch sass:css -w --no-source-map"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.51.0"
  }
}
